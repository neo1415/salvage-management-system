/**
 * Adjuster Dashboard Layout
 * 
 * Layout for Claims Adjuster pages
 */

import { ReactNode } from 'react';

export default function AdjusterLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
