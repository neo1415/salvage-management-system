/**
 * Dashboard route loading skeleton — delegates to unified PageLoadingSkeleton.
 */

import { PageLoadingSkeleton } from '@/components/ui/loading-states';

export function DashboardSkeleton() {
  return <PageLoadingSkeleton />;
}
